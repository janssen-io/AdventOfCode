defmodule Year<%= year %>.Day<%= day %> do
  @doc ~S"""
      iex> AdventOfCode.example(<%= year %>, <%= day %>)
      ...> |> Year<%= year %>.Day<%= day %>.p1
      0
  """
  def p1(lines) do
    Enum.count(lines)
  end

  @doc ~S"""
      iex> AdventOfCode.example(<%= year %>, <%= day %>)
      ...> |> Year<%= year %>.Day<%= day %>.p2
      0
  """
  def p2(lines) do
    Enum.count(lines)
  end
end